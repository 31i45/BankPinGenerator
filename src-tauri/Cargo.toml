[package]
name = "bank-pin-generator"
version = "1.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2.11", features = [] }

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
